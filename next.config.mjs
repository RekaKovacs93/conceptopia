/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.pixabay.com",
            "cdn0.iconfinder.com",
            "img.icons8.com"
        ]
    }
};

export default nextConfig;
